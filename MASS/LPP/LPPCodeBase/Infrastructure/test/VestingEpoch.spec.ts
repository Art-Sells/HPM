// Placeholder until vesting/epoch logic is implemented
import { describe, it } from "mocha";
describe.skip("Vesting and epoch caps (deferred)", () => {
  it("to be implemented once vesting logic exists", () => {});
});
// add test to see if anyone (even treasury or LPs) can change the vesting, should be no...
// Also add tests to see if vesting schedules succeed and if they pay, before and after
// and how will the smart contract count these Epochs, 
// maybe through an external API (create in To-Do after MEV testing?)
//add snapshots to verify